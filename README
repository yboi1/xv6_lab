3.15
lab RISC-V assembly:
    1· a0-a7 保存传入函数的参数
    2· 当函数的值为一个立即数时, 编译器会自动优化
    3· jalr 指令为跳转并链接指令, 跳转到目标地址, 并将当前下一条指令的地址存储下来

    简单的Hello World程序:
        usigned int i = 0x00646c72;
        printf("H%x Wo%s\n", 57616, &i);    // 57616为E110的十六进制, i中用十六进制表示ASCII码, 表示rld;

lab Backtrace:
    1* 使用r_fp()函数读取fp指针的值, fp指针存储的是栈顶的位置, (fp-8)存储的是ra返回函数的地址
    (fp-16) 存储的是保存的fp栈指针
    2* 使用 PGROUNDUP(fp)-PGROUNDDOWN(fp) == PGSIZE 条件判断是否为一个页表的起始地址