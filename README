2.8 阅读了Xv6的第六章和第七章的前三节   与jyy课上的自旋锁和睡眠锁相对应， 同时课上介绍了更多的锁， 
了解了进程的切换机制, proc结构体和cpu结构体

3.4 重新阅读了第二章， 了解了第一个进程的启动方式： 先用汇编初始化一个栈 entry.s， 然后
跳入到start.c 执行main函数前的准备工作， 例如开启中断或开启计时器中断， 然后以低权限的模式下
执行main函数。  之后去proc.c 执行userinit 创建出第一个线程， 用该线程执行init 文件， 创造出
第一个shell！ 

